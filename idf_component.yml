version: "0.1.16"
description: "ESP-IDF LEDC-based interleaved PWM generator with phase shift support"
url: "https://github.com/embedblocks/interleaved-pwm"
license: "MIT"

dependencies:
  idf: ">=5.0"

files:
   use_gitignore: true
   exclude:
      - ".git/**/*"
      - "docs/**/*"